from swiss import SwissEngine
from player import Player
import math

input_players = (
Player(name='Alice',
               rating=2300),
Player(name='Pablo',
               rating=2350),
Player(name='Qba',
               rating=2200),
Player(name='Ziom',
               rating=2100),
)

engine = SwissEngine(1, input_players)



